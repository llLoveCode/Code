/*Insert_Sort*/

/*void Insert_Sort(A)
   for j = 1 to length(A)
      do key = A[j]
      将A[j]插入到以排好序的A[0：j-1]
      i = j - 1
      while i > 0 and A[i] > key
         do A[i+1] = A[i]
         i = i -1
      A[i+1] = key
*/

void Insert_Sort()
{
    
}